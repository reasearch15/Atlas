import { AdminVerifyDevice } from "@/features/admin-auth/admin-verify-device";

/**
 * Displays the Platform Admin new-device verification page.
 */
export default function AdminVerifyDevicePage() {
  return <AdminVerifyDevice />;
}
